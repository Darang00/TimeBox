name: Keep Render Alive

on:
  schedule:
    - cron: '*/14 * * * *'  # 14분마다 실행

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Server
        run: curl https://timebox-gzom.onrender.com/